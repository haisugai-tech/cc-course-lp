# DELIVERY.md — 教材配布・受講者管理の運用手順書

販売担当者・管理者向けの運用マニュアル。教材は Google ドライブの個別共有（閲覧のみ）で配布する。

- プラン: 30万円（税込）の1プランのみ
- 販売者: 株式会社ジリーナ
- 視聴期間: 教材アクセス権の付与日から6ヶ月
- サポート: なし（動画教材の提供のみ）
- 決済: 銀行振込のみ／返金: 一切なし

---

## 1. Googleドライブ側のセットアップ（初回1回だけ）

1. Googleドライブに `CC講座_教材` フォルダを作成し、全7章の動画（MP4）・プロンプト集・完成版コードを配置する。
2. フォルダの共有設定を以下にする。
   - 一般的なアクセス: **「制限付き」**（「リンクを知っている全員」には絶対にしない）
   - 歯車アイコン →「閲覧者と閲覧者（コメント可）に、ダウンロード、印刷、コピーの項目を表示する」の**チェックを外す**
3. 受講者ごとの個別フォルダは作らない。この1つのフォルダに、受講者のGoogleアカウントを1人ずつ「閲覧者」として追加していく。

※ ダウンロード禁止設定は画面録画までは防げない。完全な防止は不可能だが、Googleアカウント紐付けにより気軽な共有はほぼ止まる。数人規模ではこれで十分と判断している。

## 2. 受講者管理スプレッドシート

`CC講座_受講者管理` という名前でスプレッドシートを作り、以下の列を持たせる。

| 列 | 見出し | 内容 |
|---|---|---|
| A | 氏名 | |
| B | Gmailアドレス | 権限付与に使用 |
| C | 申込日 | |
| D | 入金確認日 | |
| E | 権限付与日 | この日が視聴期間の起算日 |
| F | 視聴期限日 | `=EDATE(E2,6)` で自動計算 |
| G | 権限削除日 | スクリプトが自動記入 |
| H | 状態 | 有効／終了 |
| I | 紹介者 | 手数料計算用 |
| J | 備考 | |

## 3. 販売から配布までの手順

1. 申込フォームの回答を受信する（氏名・Gmailアドレス・同意チェックを含む）。
2. 案内メールを送り、振込先と支払期日（送付から7日以内）を伝える。
3. 入金を確認する。
4. `CC講座_教材` フォルダの共有に、受講者のGmailアドレスを「閲覧者」で追加する。通知メールは送る設定にする。
5. 管理スプレッドシートに、権限付与日を記入する（F列に期限が自動計算される）。
6. 受講者へ「視聴期限は◯年◯月◯日まで」と明記したメールを送る。

## 4. 視聴期限の自動剥奪（Google Apps Script）

管理スプレッドシートに紐づく Apps Script として、[scripts/expire-access.gs](scripts/expire-access.gs) を実装する（コードのApps Script側への貼り付けは人間が行う）。

- 毎日1回（例：午前3時）の時間主導型トリガーで実行する。
- 各行について、F列（視聴期限日）が今日以前 かつ H列が「有効」の行を対象とする。
- `DriveApp.getFolderById(FOLDER_ID).removeViewer(email)` で閲覧権限を削除する。
- G列に削除日を記入し、H列を「終了」に更新する。
- 実行結果を管理者のメールアドレスへ通知する（削除した件数と対象者）。
- 権限削除の3日前に、対象者へ「まもなく視聴期限です」というリマインドメールを送る。

### セットアップ手順

1. `CC講座_受講者管理` スプレッドシートを開き、「拡張機能 → Apps Script」を開く。
2. `scripts/expire-access.gs` の内容を貼り付けて保存する。
3. 「プロジェクトの設定 → スクリプト プロパティ」に以下を登録する（コードに直書きしない）。
   - `FOLDER_ID`: `CC講座_教材` フォルダのID（URLの `folders/` 以降の文字列）
   - `ADMIN_EMAIL`: 実行結果の通知先メールアドレス
4. エディタで `dailyExpireCheck` を一度手動実行し、権限承認を済ませる。
5. 「トリガー」で `dailyExpireCheck` を「時間主導型・日付ベースのタイマー・午前3時〜4時」で登録する。

## 5. 申込フォームのセットアップ

既存の申込フォームのURLは変えず、中身だけを [scripts/rebuild-form.gs](scripts/rebuild-form.gs) で作り直す。

1. 申込フォームを編集モードで開く（`https://docs.google.com/forms/d/e/1FAIpQLSfvtbbRLhF1hiYa6Op1Mq7PtWukFfTlmmpFMajYjxpNhQ-BVA/viewform` からではなく、Googleフォームの一覧から開く）。
   - 実行前の準備: フォームの回答タブから既存の回答をすべて削除しておく（回答が1件でもあるとスクリプトは中断する）。
2. 右上の「⋮」→「スクリプトエディタ」を開く。
3. `scripts/rebuild-form.gs` の内容を貼り付けて保存する。
4. `rebuildForm` を選んで実行し、権限承認ダイアログを通す。
   - ※「回答のコピーを回答者に送信」は Apps Script の API では設定できないため、実行後にフォームの「設定 → 回答 → 回答のコピーを回答者に送信: 常に表示」を手動で有効にする（実行ログにも同じ注意が出る）。
5. 実行ログに出力された公開URLが、LPに書かれているURLと一致することを確認する。
6. フォームをプレビューし、設問と同意文が指示どおりになっていること、プラン選択の設問が残っていないことを目視で確認する。
7. 回答先のスプレッドシートを `CC講座_受講者管理` とは別に作る。管理シートへの転記は手作業で行う。
8. フォームをPDFで保存し、日付を付けて保管する。
